{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "node --require ts-node/register --require tsconfig-paths/register -r dotenv/config ./src/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}

