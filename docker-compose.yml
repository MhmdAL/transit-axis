version: '3.8'

services:
  # Moveo Core API
  moveo-core:
    build:
      context: ./moveo-core
      dockerfile: Dockerfile
    container_name: moveo-core-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://moveo_user:moveo_password@moveo-postgres:5432/moveo_fleet?schema=public
      - JWT_SECRET=your-super-secret-jwt-key-here
      - PORT=3000
      - TELEMETRY_SERVICE_API_URL=http://telemetry-service:3003
    networks:
      - moveo-network
    volumes:
      - ./moveo-core:/app
      - /app/node_modules
    command: npm run dev

  # Telemetry Service
  telemetry-service:
    build:
      context: ./telemetry-service
      dockerfile: Dockerfile
    container_name: telemetry-service-api
    ports:
      - "3003:3003"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://telemetry_user:telemetry_password@telemetry-postgres:5432/moveo_telemetry?schema=public
      - PORT=3003
    networks:
      - moveo-network
    volumes:
      - ./telemetry-service:/app
      - /app/node_modules
    command: npm run dev

  # Vehicle API (Middleware between moveo-cc and moveo-core)
  vehicle-api:
    build:
      context: ./vehicle-api
      dockerfile: Dockerfile
    container_name: vehicle-api
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=development
      - PORT=3002
      - MOVEO_CORE_API_URL=http://moveo-core:3000
      - TELEMETRY_SERVICE_API_URL=http://telemetry-service:3003
    depends_on:
      - moveo-core
      - telemetry-service
    networks:
      - moveo-network
    volumes:
      - ./vehicle-api:/app
      - /app/node_modules
    command: sh -c "npm install && npm run start:dev"

  # MoveoCC-API (Real-time Vehicle Live Tracking)
  moveo-cc-api:
    build:
      context: ./moveo-cc-api
      dockerfile: Dockerfile
    container_name: moveo-cc-api
    ports:
      - "3004:3004"
    environment:
      - NODE_ENV=development
      - PORT=3004
      - SOCKET_IO_PORT=3004
      - LOG_LEVEL=debug
      - BATCH_INTERVAL_MS=1000
      - BATCH_MAX_SIZE=100
      - SOCKET_IO_PING_INTERVAL=25000
      - SOCKET_IO_PING_TIMEOUT=60000
      - SOCKET_IO_MAX_HTTP_BUFFER_SIZE=1000000
      - MOVEO_CORE_BASE_URL=http://moveo-core:3000
    depends_on:
      - moveo-core
    networks:
      - moveo-network
    volumes:
      - ./moveo-cc-api:/app
      - /app/node_modules
    command: npm run dev
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3004/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  moveo-network:
    driver: bridge

